[project]
name = "weekend-plan"
version = "0.1.0"
description = "Executable planning materials for a Tianjin to Beilingshan weekend trip."
requires-python = ">=3.12"
dependencies = [
    "streamlit>=1.57.0",
]
